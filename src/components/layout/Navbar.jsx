const Navbar = () => {
  return (
    <header>
      <ul>
        <li>{/* Logo and Page Name*/}</li>
        <li>
          <nav>{/* Links to Pokedex and Teams */}</nav>
        </li>
        <li>
          {/* User information when logged in || Register/Login buttons */}
        </li>
      </ul>
    </header>
  );
};

export default Navbar;
